# Site Creator 사용 가이드

총괄 PM(사용자)이 이 틀로 고객 홈페이지 프로젝트를 시작하고, 진행하고, 마무리하는 방법을 정리한 문서입니다.
규칙의 원문은 [CLAUDE.md](CLAUDE.md), 전체 구조 요약은 [README.md](README.md)를 참고하세요.

## 목차

1. [한눈에 보기](#1-한눈에-보기)
2. [사전 준비](#2-사전-준비)
3. [핵심 개념](#3-핵심-개념)
4. [빠른 시작](#4-빠른-시작)
5. [고객 요청 작성 가이드](#5-고객-요청-작성-가이드)
6. [명령어 레퍼런스](#6-명령어-레퍼런스)
7. [단계별 진행 가이드 (P1~P8)](#7-단계별-진행-가이드-p1p8)
8. [PM 응답 방법](#8-pm-응답-방법)
9. [산출물 찾아보기](#9-산출물-찾아보기)
10. [팀 간 협업 기록 읽는 법](#10-팀-간-협업-기록-읽는-법)
11. [변경 요청 처리](#11-변경-요청-처리)
12. [Git 운영](#12-git-운영)
13. [배포와 고객 인도](#13-배포와-고객-인도)
14. [여러 프로젝트 운영](#14-여러-프로젝트-운영)
15. [틀(에이전트·규칙·템플릿) 수정](#15-틀에이전트규칙템플릿-수정)
16. [문제 해결 FAQ](#16-문제-해결-faq)
17. [PM 체크리스트](#17-pm-체크리스트)

---

## 1. 한눈에 보기

```
PM(사용자) ──요청·승인──▶ 오케스트레이터(메인 Claude 세션) ──호출──▶ 팀 에이전트 6종
                                                                    │
                          projects/<프로젝트>/ ◀──── 산출물·리뷰·티켓 파일 ────┘
```

- **PM이 하는 일**: 고객 요청 전달 → 단계마다 게이트 보고를 보고 승인·수정 지시 → 디자인 시안 선택 → 배포 승인 → 종료 승인
- **자동으로 진행되는 일**: 문서 작성, 팀 간 교차 검토와 수정 반영, 사이트 구현, 테스트와 결함 수정, 현황판 갱신, git 커밋·태그
- **결과물 위치**: 모든 산출물은 `projects/<프로젝트 ID>/` 아래에 쌓이고, 프로젝트마다 독립된 git 저장소로 관리됩니다.

---

## 2. 사전 준비

| 항목 | 용도 | 확인 방법 |
|---|---|---|
| Claude Code | 오케스트레이터·에이전트 실행 | VS Code 확장 또는 CLI |
| Git | 틀·프로젝트 버전 관리 | `git --version` |
| Node.js (LTS) | 사이트 빌드, 테스트 도구(Lighthouse 등) 실행 | `node --version` — P4 이전에 설치 권장 |
| GitHub 계정 (선택) | 원격 저장소, 일부 호스팅 연동 | — |
| 호스팅 계정 (P7) | 운영 배포 (Vercel·Netlify·Cloudflare Pages 등) | P7 배포 계획서에서 안내 |

**세션 재시작**: 에이전트(`.claude/agents/`)·명령어(`.claude/skills/`)·설정(`.claude/settings.json`)을 새로 만들거나 수정했다면 Claude Code 세션을 다시 시작해야 반영됩니다.

---

## 3. 핵심 개념

### 3.1 틀과 프로젝트

| | 틀 (`./`) | 프로젝트 (`projects/<slug>/`) |
|---|---|---|
| 내용 | 에이전트, 명령어, 규칙, 템플릿 | 한 고객의 모든 산출물·소스·협업 기록 |
| Git | 틀 저장소 (GitHub `site-creator`) | 프로젝트별 독립 저장소 |
| 수정 시점 | PM이 **명시적으로 요청할 때만** | 프로젝트 진행 중 상시 |

프로젝트 작업 중에는 틀을 건드리지 않습니다. 템플릿은 틀에서 한 번만 관리하고, 에이전트가 템플릿을 읽어 프로젝트 안에 문서를 새로 작성합니다.

### 3.2 팀

| 팀 | 에이전트 | 주요 산출물 |
|---|---|---|
| PMO | `pmo` | 계획서, 현황판, 게이트 문서, 중간·최종 보고서 |
| 기획팀 | `planner` | 요구사항 정의서, 정보구조(IA), 화면정의서 |
| 디자인팀 | `designer` | 디자인 컨셉 시안, 디자인 시스템, 페이지 디자인, HTML 목업 |
| 개발팀 | `developer` | 기술 설계서, 사이트 소스, 개발 보고서 |
| 품질검증팀 | `qa` | 테스트 계획·케이스·결과, 결함 티켓 |
| 배포운영팀 | `devops` | 배포 계획·보고서, 운영 가이드 |

### 3.3 단계와 게이트

```
P1 계획 → P2 기획 → P3 디자인 → P4 개발 → P5 검증(로컬) → P6 중간보고 → P7 배포(운영) → P8 최종 산출물
  G1        G2         G3         G4           G5              G6              G7              G8
```

모든 단계는 같은 순서로 진행됩니다.

```
작성(Owner) → 교차 검토(다른 팀들, 동시에) → 수정 반영 (최대 3라운드) → 게이트 문서 → PM 승인 → 커밋·태그
```

**게이트**는 단계를 통과해도 되는지 PM이 판단하는 관문입니다. PM 승인 없이는 다음 단계로 넘어가지 않습니다.

### 3.4 ID 체계

| ID | 의미 | 예 |
|---|---|---|
| `CR-000` | 최초 고객 요청 | `pm/requests/CR-000_initial-request.md` |
| `CR-001`~ | 변경 요청 | |
| `REQ-F/N/C-001` | 기능 / 비기능 / 콘텐츠 요구사항 | `REQ-F-003` 예약 문의 폼 |
| `SCR-001` | 화면 | `SCR-002` 병원 소개 |
| `CMP-001` | 디자인 컴포넌트 | `CMP-001` Button |
| `TC-001` | 테스트 케이스 | |
| `TKT-{팀}-001` | 팀 간 요청·질의 티켓 | `TKT-design-001` |
| `DEF-001` | 결함 | |
| `ADR-001` | 결정 기록 | 디자인 시안 선택 결과 |
| `G1`~`G8` | 게이트 (git 태그 이름과 동일) | |

---

## 4. 빠른 시작

```text
① Claude Code 세션 시작 (틀 폴더에서)
② /kickoff hanbit-dental 강남 '한빛치과' 홈페이지. 목적: 예약 문의 증가. 페이지: 메인, 소개, 진료과목, 오시는 길, 문의. 10월 말 오픈.
③ [G1 게이트 보고 확인] → "승인"
④ /run-phase next          → P2 기획 → [G2 보고] → "승인"
⑤ /run-phase next          → P3 디자인 → [시안 선택] → [G3 보고] → "승인"
⑥ /run-phase next          … P8까지 반복
   (언제든) /status
```

---

## 5. 고객 요청 작성 가이드

### 5.1 최소 정보

**목적, 페이지 구성, 일정**만 있으면 시작할 수 있습니다. 나머지는 계획서에 `[TBD: 고객 확인 필요]`로 표시되고, PM이 고객에게 바로 물어볼 수 있는 질문 목록으로 정리됩니다.

| 항목 | 필수 | 영향 단계 |
|---|---|---|
| 고객·사이트 이름 | ✅ | 전체 |
| 사이트 목적 / 성공 기준 | ✅ | P1 범위, P2 요구사항 |
| 필요한 페이지 | ✅ | P2 정보구조·화면정의 |
| 희망 오픈 일정 | ✅ | P1 일정 |
| 주요 방문자·주 사용 기기 | | P2, P3 |
| 선호 분위기·참고 사이트 | | P3 디자인 시안 |
| 보유 자료 (로고·사진·문구) | | P2 콘텐츠, P3 에셋 |
| 필요 기능 (문의 폼·지도·게시판·다국어·예약) | | P2, P4 기술 선택 |
| 도메인·호스팅 | | P7 배포 |
| 오픈 후 콘텐츠 수정 주체 | | P4 기술 선택, P8 운영 가이드 |
| 예산 범위 | | P1, P4 기술·호스팅 선택 |

### 5.2 요청서 양식

긴 요청은 아래 양식을 파일(예: `C:\Users\<사용자>\Desktop\hanbit-request.md`)로 저장한 뒤 경로를 넘기면 됩니다. 요청 파일은 **틀 폴더 밖** 아무 곳에 두어도 되며, 내용은 `CR-000`에 원문 그대로 기록됩니다.

```markdown
# 홈페이지 제작 요청

## 기본 정보
- 고객(회사/기관명):
- 업종:

## 목적
- 사이트를 만드는 이유:
- 성공 기준: (예: 월 문의 30건, 채용 지원 증가)

## 방문자
- 주요 방문자:
- 주 사용 기기: 모바일 / PC / 비슷함

## 페이지 구성
-

## 기능
- [ ] 문의 폼   - [ ] 지도   - [ ] 공지/게시판   - [ ] 다국어   - [ ] 예약   - [ ] 기타:

## 디자인
- 선호 분위기:
- 참고 사이트와 좋은 점:
- 피하고 싶은 스타일:

## 보유 자료
- 로고: 있음 / 없음
- 사진:
- 문구 (회사 소개, 연혁 등):

## 운영
- 도메인: 보유(주소:     ) / 신규 필요 / 모름
- 호스팅 선호:
- 오픈 후 콘텐츠 수정은 누가:

## 일정 · 예산
- 희망 오픈일:
- 예산 범위:

## 기타 요청
```

### 5.3 프로젝트 ID(slug) 정하기

- 영문 소문자·숫자·하이픈, 3~40자: `hanbit-dental`, `acme-corp-homepage`, `2026-seoul-expo`
- 폴더명·git 저장소명·배포 URL 후보로 쓰이므로 짧고 알아보기 쉽게 정합니다.
- 비워 두면 오케스트레이터가 요청 내용을 보고 후보를 제안합니다.
- 같은 ID의 프로젝트가 이미 있으면 새로 만들지 않고 멈춥니다.

---

## 6. 명령어 레퍼런스

> 프로젝트가 **하나만 진행 중**이면 `project-slug`는 생략할 수 있습니다. 여러 개면 붙이거나, 생략 시 어느 프로젝트인지 질문을 받습니다.
> ⚠️ Claude Code 기본 명령 `/init`은 `CLAUDE.md`를 덮어쓰므로 **사용하지 마세요.**

### `/kickoff <project-slug> <고객 요청 | 요청 파일 경로>`

새 프로젝트를 만들고 P1 계획까지 진행합니다.

| 순서 | 동작 |
|---|---|
| 1 | slug 확인, 중복 검사, 틀 커밋 번호 확인 |
| 2 | `templates/project/`를 `projects/<slug>/`로 복사, `git init` |
| 3 | 고객 요청 원문을 `pm/requests/CR-000_initial-request.md`에 기록 |
| 4 | 최초 커밋 + 태그 `kickoff` |
| 5 | P1: pmo 계획서·킥오프 회의록 작성 → planner·developer 검토 → 게이트 G1 |
| 6 | PM에게 G1 보고 (범위, 일정, 고객 확인 질문 목록, 리스크) |

```text
/kickoff hanbit-dental 강남 한빛치과 홈페이지, 예약 문의 증가 목적, 5페이지, 10월 말 오픈
/kickoff hanbit-dental C:\Users\natio\Desktop\hanbit-request.md
```

### `/run-phase [project-slug] <P1~P8 | next>`

단계를 표준 루프로 실행하고 게이트 보고까지 진행합니다. 승인하면 커밋·태그가 자동으로 처리됩니다.

```text
/run-phase next                    # 진행 중인 프로젝트의 다음 단계
/run-phase hanbit-dental next
/run-phase hanbit-dental P5        # 특정 단계 지정
```

- 이전 게이트가 승인되지 않았으면 진행하지 않습니다. 꼭 진행해야 하면 "G3 미승인이지만 P4 진행해"처럼 명시하면 예외로 기록하고 진행합니다.

### `/status [project-slug]`

읽기 전용으로 현황을 보고합니다.

| 상황 | 보고 내용 |
|---|---|
| 프로젝트 없음 | 시작 방법 안내 |
| 진행 중 프로젝트 1개 / slug 지정 | 상세 현황: 단계 진행률, PM 결정 대기, 열린 티켓·결함, 리스크, `[TBD]` 건수, 최근 팀 활동, git 태그·미커밋 변경 |
| 여러 프로젝트 | 프로젝트 목록: ID, 이름, 고객, 상태, 현재 단계, 최근 태그 |

현황판 내용과 실제 파일 상태가 다르면 차이를 함께 알려 줍니다.

### `/change-request [project-slug] <변경 내용>`

진행 중 고객 요구 변경을 처리합니다. 자세한 내용은 [11. 변경 요청 처리](#11-변경-요청-처리)를 참고하세요.

```text
/change-request 영어 페이지도 추가해 달라고 합니다
/change-request hanbit-dental 예약 문의 폼에 희망 진료과목 선택 항목 추가
```

### 자연어 지시

명령어 없이 말로 지시해도 됩니다. 오케스트레이터가 CLAUDE.md의 프로세스에 맞춰 해석합니다.

```text
다음 단계 진행해
한빛치과 프로젝트 지금 어디까지 했어?
디자인 시안 다시 뽑아줘. 좀 더 따뜻한 느낌으로
DEF-004는 이번 오픈에서 제외하고 다음에 처리하자
P3 진행 중에 세션이 끊겼어. 기존 산출물이랑 리뷰 확인해서 이어서 진행해
```

---

## 7. 단계별 진행 가이드 (P1~P8)

경로는 모두 `projects/<slug>/` 기준입니다.

### P1 계획 — `/kickoff`로 자동 진행

| 항목 | 내용 |
|---|---|
| 작성 | pmo |
| 검토 | planner(범위 누락), developer(기술 실현 가능성·일정) |
| 산출물 | `pm/01_project-plan.md`, `shared/meetings/MTG-*_kickoff.md` |
| **PM 확인** | 범위 포함/제외가 맞는가 · 일정이 현실적인가 · **고객 확인 질문 목록** |
| **PM 결정** | G1 승인 → 태그 `G1` |

> 💡 G1 보고의 "고객 확인 필요 사항"은 질문 형태로 정리되어 나옵니다. 고객에게 전달해 답을 받아 두면 P2가 훨씬 정확해집니다. 답을 받으면 "고객 답변: …"으로 알려 주세요.

### P2 기획

| 항목 | 내용 |
|---|---|
| 작성 | planner (요구사항 → 정보구조 → 화면정의서) |
| 검토 | designer(디자인 실현성), developer(구현 가능성), qa(테스트 가능성) |
| 산출물 | `planning/02_requirements.md`, `02_information-architecture.md`, `02_storyboard.md` |
| **PM 확인** | Must 요구사항이 고객 핵심 요구와 일치하는가 · 사이트맵·메뉴 구조 · 화면별 문구 초안 · `[TBD]` 목록 |
| **PM 결정** | G2 승인 |

### P3 디자인

| 항목 | 내용 |
|---|---|
| 작성 | designer — ① 컨셉 시안 2~3안 → ② (PM 선택) → ③ 디자인 시스템·페이지 디자인·목업 |
| 검토 | planner(화면정의 반영), developer(구현 가능성) |
| 산출물 | `design/03_design-concept.md`, `03_design-system.md`, `03_page-design.md`, `design/mockups/*.html` |
| **PM 확인** | `design/mockups/concept-*.html`을 **브라우저로 직접 열어** 시안 비교 → 이후 `scr-*.html` 화면 목업 |
| **PM 결정** | ① **컨셉 시안 선택** (선택 결과는 ADR로 기록) ② G3 승인 |

> 💡 목업 HTML 파일은 탐색기에서 더블클릭하면 브라우저로 열립니다. 시안 선택 시 "B안 + A안 컬러"처럼 조합을 요청해도 됩니다.

### P4 개발

| 항목 | 내용 |
|---|---|
| 작성 | developer — ① 기술 설계 → (devops·qa 검토) → ② 구현 → ③ 로컬 빌드 확인·개발 보고서 |
| 검토 | 설계: devops, qa / 구현: designer(디자인 일치), planner(기능 부합) |
| 산출물 | `developer/04_tech-design.md`, `developer/site/`, `developer/04_dev-report.md` |
| **PM 확인** | 기술 스택 선택 이유(고객이 운영할 수 있는가, 비용) · **로컬에서 사이트 직접 확인** · 요구사항별 구현 현황 |
| **PM 결정** | G4 승인 |

로컬 실행 방법은 `developer/site/README.md`에 있습니다. 일반적인 형태:

```bash
cd projects/<slug>/developer/site
npm install
npm run dev       # 표시되는 http://localhost:xxxx 주소를 브라우저로 열기
```

### P5 검증(로컬)

| 항목 | 내용 |
|---|---|
| 작성 | qa — 테스트 계획·케이스 → 실행 → 결함(DEF) 발행 → developer 수정 → qa 재검증 (최대 3사이클) |
| 검토 | developer(결함 판정 동의), planner(요구사항 충족) |
| 산출물 | `qa/05_test-plan.md`, `05_test-cases.md`(요구사항 추적표), `05_test-report.md`, `shared/tickets/DEF-*` |
| **PM 확인** | 결론(합격/조건부/불합격) · Critical·Major 결함 0건 · **잔존 Minor 결함과 이월 사유** · 검증하지 못한 항목 |
| **PM 결정** | G5 승인, 잔존 결함 이월 여부 |

### P6 중간보고

| 항목 | 내용 |
|---|---|
| 작성 | pmo |
| 검토 | 전 팀 (사실 확인) |
| 산출물 | `pm/06_interim-report.md` |
| **PM 할 일** | 보고서로 **고객에게 중간보고** → 고객 피드백을 그대로 전달 ("고객 피드백: …") |
| **PM 결정** | G6 승인. 변경이 필요한 피드백은 CR로 처리 → 해당 단계로 회귀 |

### P7 배포(운영)

| 항목 | 내용 |
|---|---|
| 작성 | devops — 배포 계획 → (developer·qa 검토) → **PM 배포 승인** → 릴리스 태그 → 배포 → qa 스모크 테스트 → 배포 보고서 |
| 산출물 | `devops/07_deploy-plan.md`, `qa/07_smoke-test-report.md`, `devops/07_deploy-report.md` |
| **PM 확인** | 호스팅·비용 · 도메인·DNS · 롤백 계획 · **"PM 조치 필요 사항"** (계정 생성, 결제, 토큰 발급, DNS 권한) |
| **PM 결정** | ① **배포 실행 승인** (승인 시 태그 `release-v1.0.0` 후 배포) ② G7 승인 |

> ⚠️ 운영 배포, DNS 변경, 외부 계정 생성·결제, 원격 push는 **PM이 명시적으로 승인해야만** 실행됩니다. 이전 승인은 다음 배포에 이어지지 않습니다.

### P8 최종 산출물

| 항목 | 내용 |
|---|---|
| 작성 | devops(운영 가이드) + pmo(최종 보고서·회고) |
| 검토 | 전 팀 |
| 산출물 | `pm/08_final-report.md`, `devops/08_operation-guide.md`, `shared/meetings/MTG-*_retrospective.md` |
| **PM 확인** | 목표 달성도 · 인도 산출물 목록 · 운영 가이드가 고객이 따라 할 수 있는 수준인가 · 회고의 틀 개선 제안 |
| **PM 결정** | ① G8 승인 ② **고객 인도 범위** (예: 소스+운영 가이드+최종 보고서만 / 내부 리뷰·티켓 포함 여부) |

G8 승인 후 인도 패키지 `.delivery/<slug>-G8.zip`이 만들어지고 프로젝트 상태가 `종료`로 바뀝니다.

---

## 8. PM 응답 방법

### 8.1 게이트 보고에 대한 응답

게이트 보고는 이런 형태로 옵니다.

```text
## [hanbit-dental] G2 기획 게이트 보고
pmo 권고: 승인 권고
산출물: planning/02_requirements.md …
핵심 결과: …
리뷰 요약: 2라운드, Must 4건 모두 반영, Should 1건 미반영(사유 기재)
미해결·이월 사항: …
PM 결정 필요 사항: …
→ 승인 / 수정 지시 / 보류 중 선택해 주세요.
```

| 의도 | 응답 예 | 결과 |
|---|---|---|
| 승인 | `승인` | 산출물 approved·v1.0, 커밋·태그, 다음 단계 준비 |
| 조건부 승인 | `승인. 단 REQ-F-007은 Should로 낮춰서 반영해` | 조건 반영 후 승인 처리 |
| 수정 지시 | `수정: 진료과목을 과목별 상세 페이지로 나눠줘` | 게이트 문서에 기록 → Owner 수정 → 재검토 → 재보고 |
| 보류 | `보류. 고객 답변 받고 다시 진행` | 현황판에 보류 사유 기록, 대기 |
| 질문 | `Should 미반영 건은 왜 뺐어?` | 근거 설명 후 다시 결정 요청 |

### 8.2 결정 요청에 대한 응답

| 상황 | 응답 예 |
|---|---|
| 디자인 시안 선택 | `B안으로 진행` / `B안 레이아웃에 A안 컬러 조합` / `둘 다 아님. 더 밝고 친근한 느낌으로 다시` |
| 팀 간 의견 충돌 (ADR) | `옵션 A로 결정` / `pmo 권고안대로` |
| 배포 승인 | `배포 승인` (배포 대상·시점을 확인한 뒤) |
| 인도 범위 | `소스, 운영 가이드, 최종 보고서만 인도` |
| 고객 답변 전달 | `고객 답변: 로고는 다음 주 전달, 영어 페이지는 불필요` |

---

## 9. 산출물 찾아보기

```
projects/<slug>/
├── README.md                          # 프로젝트 개요, 틀 버전, git 태그 설명
├── pm/
│   ├── STATUS.md                      # ★ 현황판 — 가장 먼저 볼 문서
│   ├── WORKLOG.md
│   ├── requests/CR-000_initial-request.md, CR-001_…
│   ├── 01_project-plan.md
│   ├── gates/G1_plan.md … G8_closing.md
│   ├── 06_interim-report.md
│   └── 08_final-report.md
├── planning/  02_requirements.md · 02_information-architecture.md · 02_storyboard.md
├── design/    03_design-concept.md · 03_design-system.md · 03_page-design.md · mockups/ · assets/
├── developer/ 04_tech-design.md · 04_dev-report.md · site/ (★ 소스코드)
├── qa/        05_test-plan.md · 05_test-cases.md · 05_test-report.md · 07_smoke-test-report.md · evidence/
├── devops/    07_deploy-plan.md · 07_deploy-report.md · 08_operation-guide.md
└── shared/    tickets/ · reviews/ · decisions/ · meetings/
```

**모든 산출물 공통 헤더** — 문서 상단에서 상태를 바로 확인할 수 있습니다.

| 필드 | 의미 |
|---|---|
| `version` | 0.x 초안 → 게이트 승인 시 1.0 → 이후 변경 1.1, 1.2 … |
| `status` | `draft` 작성 중 · `in-review` 검토 중 · `revision` 수정 중 · `approved` 승인 · `superseded` 대체됨 |
| `reviewers` | 검토한 팀 |
| 문서 끝 **변경 이력** | 버전별 변경 내용과 근거(리뷰·CR) |

---

## 10. 팀 간 협업 기록 읽는 법

에이전트끼리는 직접 대화할 수 없어서, 모든 소통이 `shared/`에 파일로 남습니다. "왜 이렇게 됐지?"가 궁금할 때 여기서 찾으면 됩니다.

| 알고 싶은 것 | 볼 곳 |
|---|---|
| 누가 어떤 문제를 지적했고 어떻게 반영됐나 | `shared/reviews/{단계}_{대상}_{검토자}_r{라운드}.md` — 지적 사항 표의 "처리 결과" 열 |
| 작업 중 팀끼리 무엇을 요청했나 | `shared/tickets/TKT-*` |
| 어떤 결함이 있었고 어떻게 고쳤나 | `shared/tickets/DEF-*` — 재현 절차, 원인, 조치, 재검증 결과 |
| 무엇을 왜 그렇게 결정했나 | `shared/decisions/ADR-*` — 선택지, 팀 의견, 결정자 |
| 각 팀이 실제로 어떤 과정을 거쳤나 | `<팀>/WORKLOG.md` |

**리뷰 지적 등급**: `Must` 반드시 수정(게이트 차단) · `Should` 권장(미반영 시 사유 필수) · `Could` 제안
**결함 심각도**: `Critical` 사이트 사용 불가·보안 · `Major` 주요 기능 오류 · `Minor` 부분 오류 · `Trivial` 오탈자
**리뷰 3라운드**를 넘기거나 팀 의견이 충돌하면 ADR로 정리되어 PM 결정 요청이 옵니다.

---

## 11. 변경 요청 처리

```text
/change-request [slug] 변경 내용
  → CR 문서에 원문 기록
  → planner·developer(필요 시 designer) 영향 의견
  → pmo 영향도 분석: 영향 요구사항·화면·산출물, 되돌아갈 단계, 일정·리스크, 권고
  → PM 결정 (수용 / 부분 수용 / 보류 / 거절) → 커밋
  → 수용 시: 영향 받는 가장 앞 단계로 회귀 → 영향 부분만 개정·재검토 → 재승인 태그 G{n}-CR-{nnn}
```

| 변경 예 | 보통 되돌아가는 단계 |
|---|---|
| 문구·이미지 교체 | P4 (개발) 또는 P2 콘텐츠 |
| 컬러·폰트 변경 | P3 (디자인) |
| 페이지 추가·기능 추가 | P2 (기획) |
| 이미 배포된 뒤의 변경 | 위 단계 + P5 재검증 + P7 재배포(새 `release-v*`) |

> 💡 에이전트·명령어·템플릿 자체를 바꾸고 싶은 것은 CR이 아니라 **틀 수정**입니다 → [15절](#15-틀에이전트규칙템플릿-수정)

---

## 12. Git 운영

### 12.1 두 저장소

| | 틀 저장소 | 프로젝트 저장소 |
|---|---|---|
| 위치 | `./` | `projects/<slug>/` |
| 원격 | `origin` = GitHub `nationminu/site-creator` | 기본 없음 (필요 시 PM 요청으로 연결) |
| 커밋 | 틀 수정 요청 처리 후 | 오케스트레이터가 자동 (아래 표) |
| push | PM 요청 시 | **PM 승인 시에만** |

틀 저장소의 `.gitignore`가 `projects/*`를 제외하므로 프로젝트 내용이 틀 저장소에 올라가지 않습니다.

### 12.2 프로젝트 저장소 자동 커밋·태그

| 시점 | 커밋 메시지 | 태그 |
|---|---|---|
| kickoff | `chore(kickoff): 프로젝트 생성 및 고객 요청 기록` | `kickoff` |
| 게이트 승인 | `gate(G{n}): {단계명} 승인` | `G{n}` |
| CR 회귀 후 재승인 | `gate(G{n}): …` | `G{n}-CR-{nnn}` |
| CR 결정 | `cr(CR-{nnn}): {요약} — {결정}` | — |
| 배포 승인 | `release: v{x.y.z} 배포 승인` | `release-v{x.y.z}` |
| 프로젝트 종료 | `chore(close): 프로젝트 종료` | — |
| 중간 저장 요청 | `wip: {요약}` | — |

에이전트는 커밋하지 않습니다. 커밋은 오케스트레이터만 하며, 커밋 전에 `.env`·비밀 정보·`node_modules`가 포함되지 않았는지 확인합니다.

### 12.3 PM이 쓸 만한 git 명령

```bash
# 태그(게이트 이력) 보기
git -C projects/hanbit-dental tag --sort=creatordate

# 커밋 이력
git -C projects/hanbit-dental log --oneline --decorate

# G2 승인 시점과 G3 승인 시점 사이에 바뀐 파일
git -C projects/hanbit-dental diff G2 G3 --stat

# G2 승인 당시의 요구사항 정의서 보기
git -C projects/hanbit-dental show G2:planning/02_requirements.md

# 이 프로젝트가 따른 틀 버전 확인 후, 그 시점의 규칙 보기
git -C projects/hanbit-dental show kickoff --stat      # 커밋 메시지의 framework: <커밋>
git show <틀 커밋>:CLAUDE.md
```

### 12.4 프로젝트 원격 저장소 연결

GitHub 등에 빈 저장소를 만든 뒤 요청합니다.

```text
hanbit-dental 프로젝트 원격 저장소 연결하고 push해줘: https://github.com/<계정>/hanbit-dental.git
```

- 고객 인도용 저장소라면 내부 리뷰·티켓·WORKLOG가 함께 올라간다는 점을 고려하세요. 공개 범위를 조절하려면 [13.3 인도 패키지](#133-고객-인도-패키지)를 사용합니다.

---

## 13. 배포와 고객 인도

### 13.1 배포 흐름

```
배포 계획서 작성·검토 → [PM 배포 승인] → release-v1.0.0 태그 → devops 배포 → 기본 확인(HTTP·HTTPS)
→ qa 운영 스모크 테스트 → 배포 보고서 → [G7 승인]
```

- 사이트 소스가 `developer/site/`에 있으므로, 호스팅 설정에서 **기준 디렉토리(Root/Base directory)** 를 `developer/site`로 지정합니다. 배포 계획서에 방법이 명시됩니다.
- 계정 로그인·토큰 발급·결제·DNS 설정처럼 사람이 해야 하는 일은 "PM 조치 필요 사항"으로 안내됩니다. 에이전트가 인증 정보를 추측하거나 우회하지 않습니다.

### 13.2 롤백

- 운영에 문제가 생기면 배포 계획서의 롤백 기준에 따라 devops가 판단하고 PM에게 보고합니다.
- 롤백은 **이전 `release-v*` 태그** 기준으로 재배포합니다.

### 13.3 고객 인도 패키지

G8 승인 후 PM이 범위를 정하면 승인 시점(`G8` 태그) 기준으로 선택한 경로만 zip으로 묶습니다.

```text
인도 범위: 소스코드, 운영 가이드, 최종 보고서
→ projects/<slug>/.delivery/<slug>-G8.zip   (git 제외 폴더)
```

---

## 14. 여러 프로젝트 운영

- 프로젝트마다 `projects/<slug>/` 폴더와 git 저장소가 따로 있어 서로 섞이지 않습니다. ID 번호(REQ, DEF 등)도 프로젝트마다 1번부터 시작합니다.
- 진행 중 프로젝트가 2개 이상이면 명령에 slug를 붙이세요: `/run-phase hanbit-dental next`
- `/status` (인자 없이) → 전체 프로젝트 목록
- 에이전트는 다른 프로젝트 폴더를 **읽기만** 할 수 있습니다. 이전 프로젝트 산출물을 참고시키고 싶다면: `acme-corp 프로젝트의 디자인 시스템 구조를 참고해서 작성해`
- **보관·중단**: `hanbit-dental 프로젝트 보류로 바꿔줘` → 현황판 상태가 `보류`로 바뀝니다.
- **삭제**: 전용 명령은 없습니다. 필요하면 `projects/<slug>/` 폴더를 직접 삭제하세요. 원격 저장소에 push해 두지 않았다면 복구할 수 없습니다.

---

## 15. 틀(에이전트·규칙·템플릿) 수정

### 15.1 보호 영역

`CLAUDE.md`, `README.md`, `.gitignore`, `.gitattributes`, `.claude/**`, `templates/**`

- 프로젝트 작업 중에는 수정·삭제하지 않습니다.
- 보호 영역을 편집하려고 하면 Claude Code가 **확인을 요청**합니다(`.claude/settings.json`).
  - 프로젝트 작업 중에 확인 요청이 뜨면 → **거절**하세요. 무엇을 왜 고치려 했는지 물어보면 됩니다.
  - 틀 수정을 직접 요청한 경우 → 내용을 확인하고 승인하세요.
- 한계: 확인 요청은 파일 편집 도구에만 적용됩니다. 셸 명령(`rm`, `cp` 등)은 CLAUDE.md 규칙으로만 막습니다.

### 15.2 수정 요청 방법

프로젝트 명령어가 아닌 일반 요청으로 말하면 됩니다.

```text
qa 에이전트에 Playwright로 스크린샷 증거를 남기는 규칙 추가해줘
요구사항 정의서 템플릿에 '법적 고지' 섹션 추가해줘
P3 단계에서 컨셉 시안을 2안으로 줄여줘
```

처리 흐름: 요청 범위만 수정 → 변경 요약 보고 → 틀 저장소 커밋(`framework: …`) → push 여부 확인 → **세션 재시작 후 반영**

### 15.3 회고 반영 사이클

```
프로젝트 P8 회고록의 "틀 개선 제안" 표 → PM 검토 → "회고 제안 1, 3번 틀에 반영해줘" → 틀 커밋 → 다음 프로젝트부터 적용
```

이미 진행 중인 프로젝트는 kickoff 때 기록된 틀 버전의 규칙으로 계속 진행하는 것이 원칙입니다. 새 규칙을 진행 중 프로젝트에 적용하려면 명시적으로 지시하세요.

---

## 16. 문제 해결 FAQ

**Q. `/kickoff` 같은 명령어가 목록에 없어요.**
Claude Code 세션을 재시작하세요. 틀 폴더(`site-creator`)를 작업 폴더로 열었는지도 확인하세요.

**Q. 작업 중에 파일 편집 확인 요청이 계속 떠요.**
보호 영역(15.1) 편집 시도입니다. 프로젝트 작업 중이라면 거절하세요. 프로젝트 폴더(`projects/…`) 편집에서 뜬다면 사용 중인 권한 모드 설정을 확인하세요.

**Q. 세션이 중간에 끊겼어요. 처음부터 다시 해야 하나요?**
아닙니다. 산출물·리뷰·티켓이 모두 파일로 남아 있습니다. `/status`로 현재 상태를 확인한 뒤 이어서 진행하도록 지시하세요.
```text
P4 진행 중에 끊겼어. 기존 산출물, 리뷰, WORKLOG 확인해서 멈춘 지점부터 이어서 진행해
```

**Q. "이전 게이트 미승인"이라며 진행을 안 해요.**
의도된 동작입니다. 해당 게이트를 먼저 승인하거나, 불가피하면 "G3 미승인 상태로 P4 진행해"라고 명시하세요. 현황판 예외 기록에 남습니다.

**Q. 리뷰가 계속 반복돼요.**
3라운드가 넘으면 자동으로 멈추고 ADR로 쟁점을 정리해 PM 결정을 요청합니다. 그 전에 끊고 싶다면 "현재 Must 지적은 이렇게 결정: …"처럼 직접 결정해 주세요.

**Q. 에이전트가 고객 정보(연혁, 전화번호 등)를 임의로 채웠어요.**
규칙 위반입니다. 확인되지 않은 사실은 `[TBD: 고객 확인 필요]`로 표시해야 합니다. 해당 문서와 위치를 알려 수정을 지시하세요. 반복된다면 회고의 틀 개선 제안으로 남기세요.

**Q. 틀 루트에 `package.json`이나 `node_modules`가 생겼어요.**
규칙 위반입니다(명령은 프로젝트 폴더 안에서만 실행). 틀 저장소 `git status`로 확인하고 정리를 지시하세요. `node_modules/`는 틀 `.gitignore`에 들어 있어 커밋되지는 않습니다.

**Q. 토큰 사용량이 많아요.**
단계마다 작성 1회 + 검토자 2~5명이 동시에 실행되고 수정 라운드가 반복되므로 사용량이 큽니다. 줄이는 방법:
- 요청을 구체적으로 작성해 리뷰 라운드를 줄이기 (특히 5절의 최소 정보)
- 작은 프로젝트는 검토자를 줄이도록 지시: "이번 프로젝트 P2 검토는 developer만"
- 게이트 보고 전에 방향을 먼저 확인받도록 지시: "요구사항 초안만 먼저 보여줘"

**Q. 배포를 에이전트가 마음대로 하지 않나요?**
운영 배포는 PM이 "배포 승인"이라고 명시하고, 오케스트레이터가 승인 일시와 릴리스 태그를 devops에 전달한 경우에만 실행됩니다. 승인 없이 호출되면 devops는 준비까지만 하고 멈춥니다.

**Q. 프로젝트에만 필요한 문서 양식이 있어요.**
템플릿을 고치지 말고 프로젝트 안에 문서를 추가하도록 지시하세요. 여러 프로젝트에 필요하다면 틀 수정으로 템플릿에 반영하세요.

---

## 17. PM 체크리스트

### 착수 전
- [ ] Claude Code 세션을 틀 폴더에서 시작했다
- [ ] 고객 요청에 목적·페이지·일정이 있다
- [ ] 프로젝트 ID(slug)를 정했다

### 게이트마다
- [ ] pmo 권고와 근거를 읽었다
- [ ] Must 지적이 모두 해결되었는지, Should 미반영 사유가 납득되는지 확인했다
- [ ] 미해결·이월 사항을 받아들일 수 있다
- [ ] `[TBD]` 중 고객에게 물어볼 것을 전달했다

### 단계별 핵심
- [ ] **G1** 범위 포함/제외와 일정에 동의한다
- [ ] **G2** Must 요구사항이 고객 핵심 요구와 일치한다
- [ ] **P3** 컨셉 목업을 브라우저로 직접 열어 보고 선택했다
- [ ] **G4** 로컬에서 사이트를 직접 확인했다
- [ ] **G5** Critical·Major 결함 0건, 잔존 결함 이월에 동의한다
- [ ] **G6** 고객 중간보고를 했고 피드백을 전달했다
- [ ] **P7** 배포 계획의 비용·도메인·롤백을 확인하고 PM 조치 사항을 처리한 뒤 배포를 승인했다
- [ ] **G8** 운영 가이드를 확인하고 인도 범위를 결정했다

### 종료 후
- [ ] 회고의 틀 개선 제안을 검토했다
- [ ] 반영할 제안을 틀 수정으로 요청했다
- [ ] 필요하면 프로젝트 저장소를 원격에 보관했다
